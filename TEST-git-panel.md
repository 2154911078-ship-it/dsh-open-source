# Git 面板测试文件

这个文件用来验证 DSH Git 面板的各个功能。测试完可以直接删掉。

## 验收清单

- [ ] 「改动」页签出现这一行：`?? TEST-git-panel.md`（灰蓝色 `?` = 未跟踪）
- [ ] 点这一行 → 自动跳到「差异」页签，显示本文件内容预览
- [ ] 点「暂存全部」→ 该行状态变成已暂存（`A`，绿色）
- [ ] 在提交框输入信息 → 回车或点「提交」→ 改动区清空
- [ ] 「提交」页签多出一条新记录

## 关于这个插件

| 项 | 说明 |
|---|---|
| host 路由 | `/dsh-git-panel/status`、`/diff`、`/branches`（只读）+ `/stage`、`/unstage`、`/commit`（写） |
| 面板位置 | 右侧栏标签页（与「文件」「文档预览」并列），右上角 `⑂ Git` 按钮作为入口 |
| git 来源 | 便携版 MinGit：`D:\桌面\deepseek\tools\git\cmd\git.exe` |
| 刷新 | 面板打开时每 10 秒自动刷新，也可点 `↻` 手动刷新 |

生成时间：2026-09-12
